* **Program Purpose**: Calculates the area of a circle, along with the surface area and volume of a sphere based on a user-provided radius.


* **Input**: Takes a single numerical radius entered by the user.


* **Output**: Displays the calculated area, surface area, and volume formatted to two decimal places.


* **Execution**: Compile the code using a C compiler (`gcc`), run the executable, and type the radius when prompted.