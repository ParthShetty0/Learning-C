Planetary Quiz Game in C
==========================
A simple console-based planetary quiz game implemented in C using 2D arrays and for loops.
The program loops through multiple-choice questions, takes user input (A/B/C/D), and checks answers.
It calculates and displays the user's final score and performance at the end.
Compile and run using: gcc quiz.c -o quiz && ./quiz